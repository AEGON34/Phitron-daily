#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    double a, c;
    int b, d, e;
    cin >> a >> b >> c;
    d = (c + .5) / a;
    e = d * b;
    cout << e << endl;
    return 0;
}