#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, k, l;
    cin >> n >> k;
    l = (n - k) * 10000;
    cout << l << endl;
    return 0;
}